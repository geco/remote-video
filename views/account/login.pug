extends ../layout

block content
  .page-header
    h3 Sign in
  form.form-horizontal(method='POST')
    input(type='hidden', name='_csrf', value=_csrf)
    .form-group
      .col-sm-5
        a.btn.btn-block.btn-lg.btn-google.btn-social(href='/auth/google')
          i.fa.fa-google-plus
          | Sign in with Google
